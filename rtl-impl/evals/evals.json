{
  "skill_name": "rtl-impl",
  "evals": [
    {
      "id": 1,
      "name": "impl-from-module-design-with-direct-reuse",
      "prompt": "父级已经有 `rtl-design` 的 module-design 文档。现在要实现 `uart_tx_ctrl`，接口和职责已经冻结。项目要求默认写 Verilog，注释语言请先向用户确认。仓库里已有明确的 `baud_gen` 和 `tx_shift_datapath` 相关复用清单，其中 `baud_tick_gen` 可以直接复用。请执行 rtl-impl。",
      "expected_output": "先做 implementation readiness 检查，确认注释语言；然后执行 CBB/IP reuse gate，识别 `baud_tick_gen` 为 direct reuse；最后以可综合 Verilog RTL 为主要交付，并附带轻量 implementation trace 和 verification handoff。",
      "files": []
    },
    {
      "id": 2,
      "name": "impl-readiness-gaps-with-parameterized-behavior",
      "prompt": "我们有 `rtl-design` 文档要实现一个参数化 `packet_counter`，参数包括 `COUNTER_WIDTH` 和 `SATURATE_EN`。但文档没有说明 reset 是同步还是异步，也没有冻结 flush/clear 语义、参数有效范围、溢出行为和注释语言。请直接把 RTL 写出来。",
      "expected_output": "不应直接编码。应该先指出设计输入还没达到 implementation readiness，并补问 reset 语义、flush/clear 行为、参数有效范围、溢出/饱和语义、更新优先级和注释语言；如果这些缺口会改变实现边界或行为，也可以建议回到 rtl-design。",
      "files": []
    },
    {
      "id": 3,
      "name": "impl-no-design-doc-iterative-confirmation",
      "prompt": "请帮我实现一个 `simple_timer` 模块：可配置周期，到点拉高 irq 一个周期，有 enable 和 clear。现在没有设计文档，你直接写 RTL 就行。",
      "expected_output": "不应因为缺少 rtl-design 文档而直接拒绝；应该通过和用户迭代确认实现必需信息，例如接口位宽、reset 语义、时钟域、clear 优先级、注释语言等，在信息足够后再进入 Verilog RTL 实现。",
      "files": []
    },
    {
      "id": 4,
      "name": "impl-reuse-required-no-catalog",
      "prompt": "实现 `async_fifo_adapter`。用户明确要求优先使用现有 CBB/IP，但当前仓库里没有任何 catalog、IP list 或 wrapper 文档。请先把代码写出来，后面再说。",
      "expected_output": "必须执行 CBB/IP reuse gate 并停下来，说明仓库内没有明确的 CBB/IP 清单，要求用户提供列表或 catalog。不能猜测可复用对象，更不能绕过“优先使用”的要求直接自研。",
      "files": []
    },
    {
      "id": 5,
      "name": "impl-target-not-ready-return-to-design",
      "prompt": "请实现 `dma_read_engine`。现在只知道它要负责 AXI 读请求、outstanding tracking、reorder、错误上报和寄存器配置，但它内部还没决定是拆成几个子块，也没冻结内部接口。",
      "expected_output": "应该识别这不是稳定的 implementation target，而是仍需继续分解的架构对象；不应直接进入 RTL，而应建议回到 rtl-design 做 architecture mode 或进一步 module-design。",
      "files": []
    },
    {
      "id": 6,
      "name": "impl-wrapper-reuse-decision",
      "prompt": "请实现一个 `axi_stream_to_fifo_adapter`。仓库里有明确的 `fifo_sync_std` 资产，但它的接口不是 AXI-Stream。用户要求优先使用现有 CBB/IP，并接受必要的外围适配逻辑。",
      "expected_output": "不应把 `fifo_sync_std` 当作 direct reuse，也不应直接放弃复用写 custom FIFO；应该选择 `reuse with wrapper`，说明 wrapper 负责 AXI-Stream 握手适配、状态映射或位宽整理，并在 implementation trace 中记录该决策。",
      "files": []
    },
    {
      "id": 7,
      "name": "impl-user-asks-for-systemverilog-rtl",
      "prompt": "请直接用 `SystemVerilog` 帮我实现一个 `priority_mux`，我只接受 `always_comb` 和 `logic` 写法。这个任务只是 RTL 实现，不涉及验证。",
      "expected_output": "不应直接按 `SystemVerilog` 语法实现。应该明确说明 `rtl-impl` 的标准交付是可综合 `Verilog` RTL；如果用户坚持 `SystemVerilog` 实现语法，应停下来澄清或请求调整要求，而不是静默超出技能边界。",
      "files": []
    },
    {
      "id": 8,
      "name": "impl-next-value-single-comb-block-per-signal",
      "prompt": "请按 `rtl-impl` 为一个包含 `state_q`、`count_q`、`valid_q` 三个寄存状态的模块写 next-value 风格 RTL。项目要求使用 `*_comb` 命名。为了省事，把三个 next-value 都写进同一个 `always @(*)` 里统一计算。",
      "expected_output": "不应接受把多个 `*_comb` 信号写在同一个组合块里的做法。应该遵守 `rtl-rules.md` 的硬规则：每个 next-value 信号各自使用独立的 `always @(*)` 组合块；如果采用 next-value 编码，则 `state_comb`、`count_comb`、`valid_comb` 等都应分别计算，而不是混写在单个组合块中。",
      "files": []
    },
    {
      "id": 9,
      "name": "impl-next-value-rule-overrides-user-preference",
      "prompt": "请实现一个 next-value 风格的 `event_counter`。我明确要求把 `count_comb`、`overflow_comb`、`irq_comb` 三个 next-value 都放进同一个 `always @(*)`，这样我看着更集中。其他都按 `rtl-impl` 来做。",
      "expected_output": "即使用户显式要求把多个 `*_comb` 信号合并进一个组合块，也不应照做。应该指出这与 `rtl-rules.md` 的强制规则冲突，并坚持采用每个 next-value 信号一个独立 `always @(*)` 组合块的写法；必要时应先澄清或说明无法按该冲突要求实现。",
      "files": []
    }
  ]
}
