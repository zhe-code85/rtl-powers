interface:
  display_name: "RTL Implementation"
  short_description: "Verilog RTL with reuse gates and implementation trace"
  default_prompt: "Use $rtl-impl to confirm implementation readiness, decide CBB/IP reuse, and produce synthesizable Verilog RTL plus a lightweight implementation trace."

policy:
  allow_implicit_invocation: true
